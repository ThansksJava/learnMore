package com.learn.practice;

/**
 * 大整数除法：基本的思想是反复做减法，看看从被除数里最多能减去多少个除数，商就是多少
 * @author fengjie
 * @version 1.0
 * @date Created in 2019/6/19 14:47
 */
public class BigInteger {
    class BigInt implements Comparable<BigInt>{
        int [] data = null;
        int symabol = 1;
        @Override
        public int compareTo(BigInt o) {
            return 0;
        }
    }
}
